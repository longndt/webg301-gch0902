{% extends 'base.html.twig' %}

{% block title %}Student{% endblock %}

{% block body %}
    <h1>Student</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ student.id }}</td>
            </tr>
            <tr>
                <th>Sid</th>
                <td>{{ student.sid }}</td>
            </tr>
            <tr>
                <th>Sname</th>
                <td>{{ student.sname }}</td>
            </tr>
            <tr>
                <th>Sdob</th>
                <td>{{ student.sdob ? student.sdob|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sgrade</th>
                <td>{{ student.sgrade }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('student_index') }}">back to list</a>

    <a href="{{ path('student_edit', {'id': student.id}) }}">edit</a>

    {{ include('student/_delete_form.html.twig') }}
{% endblock %}
