{% extends 'base.html.twig' %}

{% block body %}
<div class="container col-md-5 mt-4 text-center">
 <a href="{{ path ('product_add') }}">
    <img src="{{ asset ('images/add.png')}}" weight="50" height="50">
 </a>
 <table class="table table-primary mt-3">
    <tr>
        <th>Id</th>
        <th>Name</th>
        <th>Menu</th>
    </tr>
    {% for p in product %}
        <tr>
            <td>{{ p.id }}</td>
            <td>{{ p.name }}</td>
            <td>
                <a class="btn btn-info" href="{{ path('product_detail' , { 'id': p.id }) }}">Detail</a>
                <a class="btn btn-warning" href="{{ path('product_edit', { 'id': p.id }) }}">Edit</a>
                <a class="btn btn-danger" href="{{ path('product_delete', { 'id': p.id }) }}"
                onclick="return confirm('Do you want to delete this product ?')"     
                >Delete</a>
            </td>
        </tr>
    {% endfor %}
 </table>
</div>
{% endblock %}
